# Used by docker-compose.yml to provision the PostgreSQL container.
# Docker Compose reads this file for ${...} interpolation — see README.md.
DB_USER="admin"
DB_PASSWORD="change-me"
DB_NAME="csgrind"

# Production only (docker-compose.prod.yml) — see docs/deploiement.md
# SITE_DOMAIN="cs-grind.com"
# API_DOMAIN="api.cs-grind.com"
# VITE_API_URL="https://api.cs-grind.com"
